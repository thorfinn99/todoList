<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full h-full text-white bg-zinc-800" >
    <div class="w-full h-full text-white p-10" >
        <div class="w-full flex justify-end" >
            <a class="px-3 py-2 bg-red-500 rounded-md" href="/logout">Logout</a>
        </div>
        <div class="flex items-start items-center gap-4" >
            <div class="w-10 h-10 overflow-hidden  rounded-md" >
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl mt-2 mb-3 " > Hello <%= user.name %> </h3>
        </div>
        <h5 class="mb-3" >You Can Create A New Post.</h5>
       <form method="post" action="/post">
        <textarea  placeholder="What's On Your Mind" class="resize-none block mb-3 p-3 w-3/5 bg-transparent b-2 border-zinc-600 rounded-md" name="content" ></textarea>
        <input class="px-3 py-2 w-40 bg-blue-500 block rounded-md" value="Create New Post" type="submit">
       </form>
       <div class="mt-20" >
        <h3 class="text-zinc-400" >Your Posts</h3>
        <div class="posts flex gap-5 flex-wrap mt-5 " >
          <% user.posts.reverse().forEach(function(post){ %>
            <div class="post rounded-md w-1/3 p-4 border-[1px] bg-zinc-800  boder-zinc-700 " >
                <h4 class="text-blue-500 mb-2" >@<%= user.username %></h4>
                <p class="text-sm tracking-tight" ><%= post.content %></p>
                 <small> <%= post.likes.length %> Likes </small>
                <div class="btns flex gap-4 mt-5 " >
                    <a class="text-blue-500" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id)? "Like" : "Unlike" %>
                    </a>
                    <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                </div>  
              </div>
          <% }) %>
        </div>
       </div>
    </div>
</body>
</html>