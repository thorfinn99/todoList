<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full h-full text-white bg-zinc-800" >
    <div class="w-full h-full text-white p-10" >
        <div class="w-full flex justify-end" >
            <a class="px-3 py-2 bg-red-500 rounded-md" href="/logout">Logout</a>
        </div>
       <h5 class="mb-3" >Edit This Post</h5>
       <form method="post" action="/update<%= post._id %>">
        <textarea  placeholder="What's On Your Mind" class="resize-none block mb-3 p-3 w-3/5 bg-transparent b-2 border-zinc-600 rounded-md" name="content" > <%= post.content %> </textarea>
        <input class="px-3 py-2 w-40 bg-yellow-500 text-black block rounded-md" value="Update Post" type="submit">
       </form>
    </div>
</body>
</html>